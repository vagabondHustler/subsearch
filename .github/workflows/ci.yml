name: Continuous integration
run-name: Continuous integration

# Runs on every PR into dev:
#   dependency_review / pr_labeler always run; tox + lint only run when src/ changed.

on:
  pull_request:
    branches:
      - dev

jobs:
  # Flag known-vulnerable or disallowed dependency changes in the PR.
  dependency_review:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: "Dependency Review"
        uses: actions/dependency-review-action@v4

  # Apply labels based on the source branch name (.github/configs/labeler.yml).
  pr_labeler:
    runs-on: windows-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Auto label
        uses: TimonVS/pr-labeler-action@v5
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          configuration-path: .github/configs/labeler.yml

  # Gate the expensive jobs below on whether any src/ file changed.
  paths_filter:
    runs-on: windows-latest
    outputs:
      src: "${{ steps.filter.outputs.src }}"
    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          filters: |
            src:
              - "src/**"

  # Run the test suite via tox.
  tox:
    needs: paths_filter
    if: ${{ needs.paths_filter.outputs.src == 'true' }}
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: ["3.14.*"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tox tox-gh-actions

      - name: Run tests
        run: tox

  # Run the pre-commit hooks (black, isort, mypy, ...).
  lint:
    needs: paths_filter
    if: ${{ needs.paths_filter.outputs.src == 'true' }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Run pre-commit hooks
        run: |
          python -m pip install --upgrade pip pre-commit
          pre-commit run --all-files --show-diff-on-failure

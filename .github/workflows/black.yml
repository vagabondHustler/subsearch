name: Lint

on: [push, pull_request]

jobs:
  lint:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          pip install black
          echo black ./src --check -l 125
          black ./src --check -l 125



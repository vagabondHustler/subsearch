name: Setup Python
description: Configure Python with pip cache

runs:
  using: composite
  steps:
    - uses: actions/setup-python@v6
      with:
        python-version-file: .python-version
        cache: pip
        cache-dependency-path: pyproject.toml
